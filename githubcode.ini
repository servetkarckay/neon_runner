FRONTEND (pazar-analiz-frontend)
# 🔽 Projeyi indir
git clone git@github.com:servetkarckay/neon_runner.git
cd neonrunner

# 🏷 İsteğe bağlı geliştirme branch'i aç
git checkout -b main

# ✍ düzenlemeleri yap → sonra kaydet → terminalde:
git add .
git commit -m "Güncelleme"

git pull origin main --rebase
git push origin main
